import { GlobalStyles } from '@mui/material';

const inputGlobalStyles = (
  <GlobalStyles
    styles={{
      body: {
        margin: 0,
        padding: 0,
      },
    }}
  />
);

export default inputGlobalStyles;
